import {test,expect} from '@playwright/test';test('logs and completes a workout',async({page})=>{await page.goto('/');await page.getByLabel('Workout').click();await page.getByTestId('start-workout').click();for(const i of [0,1]){await page.getByTestId(`set-${i}-weight`).fill('135');await page.getByTestId(`set-${i}-reps`).fill('8');await page.getByTestId(`set-${i}-done`).check()}for(let i=0;i<5;i++)await page.getByRole('button',{name:'Next exercise'}).click();await page.getByTestId('finish-workout').click();await expect(page.getByTestId('workout-summary')).toBeVisible();await page.getByRole('button',{name:'Back to history'}).click();await expect(page.getByTestId('history')).toBeVisible()});
