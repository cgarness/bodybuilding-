# Physique OS contributor notes
- Keep business rules in `src/domain` and cover changes with Vitest.
- The app is single-user and local-first; do not add authentication or remote storage.
- Maintain strict TypeScript, accessible labels, large mobile tap targets, and dark/light contrast.
- Run `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` before committing.
