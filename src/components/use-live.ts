"use client";import { useEffect,useRef,useState } from "react";export function useLive<T>(query:()=>Promise<T>){const [data,setData]=useState<T>();const queryRef=useRef(query);queryRef.current=query;useEffect(()=>{let active=true;const run=()=>queryRef.current().then(value=>{if(active)setData(value)});run();const timer=setInterval(run,700);return()=>{active=false;clearInterval(timer)}},[]);return data}
